# Aureon desktop launcher — HOLD

This legacy desktop build, setup wizard, credential route, GUI, trading start,
external dashboard, and packaging path are disabled. The active Python entry
files emit machine-readable `HOLD` receipts; the batch builder and PyInstaller
spec terminate without installing dependencies or creating an executable.

Do not reactivate this surface until a signed, reproducible native package and
outer protection boundary have been independently reviewed.
