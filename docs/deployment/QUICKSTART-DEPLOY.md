# Aureon quick deployment: terminal HOLD

All former one-command provider, container, service, and live-trading routes are
withdrawn. The only supported operation is verification that each official
entrypoint returns `decision: HOLD` and `process_start_authorized: false`.

This receipt is local fail-closed evidence, not proof of deployment or full-OS
protection.
