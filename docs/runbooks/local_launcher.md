# Local launcher — HOLD

The local launcher, setup wizard, dashboard activation, trading start, and
autostart registration routes are disabled. `cli.launcher` emits a
machine-readable `HOLD` receipt and performs no configuration, credential,
process, listener, browser, or operating-system mutation.

Do not package or reactivate the historical route until the native protection
boundary and full source census are complete and independently reviewed.
