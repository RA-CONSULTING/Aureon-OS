# DO NOT DEPLOY

This directory is an evidence-only archive of eleven Edge Functions retrieved
read-only from Supabase project `siihxcwetdjdsrfdexmb` on 2026-08-11.

The archived source was canonicalized only to LF line endings with one final
newline. Each SHA-256 in `manifest.json` was recomputed from a fresh provider
read-back and matched the corresponding local archive byte-for-byte after that
declared normalization.

These files preserve the unsafe provider state for audit and recovery. They
must not be copied into `supabase/functions`, bundled, served, or deployed.
The deploy-tree files with the same slugs are inert quarantine handlers.

No table row content, provider secret value, deployment, deletion, DDL, or
provider mutation was used to create this archive.
